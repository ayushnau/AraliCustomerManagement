# Port the HTTP server listens on.
PORT=5001

# Comma-separated whitelist of allowed CORS origins.
# Leave empty to allow any origin (only do this in dev).
CORS_ORIGINS=http://localhost:3000

# morgan log format: "dev" | "combined" | "tiny" | "common" | "short"
LOG_FORMAT=dev
